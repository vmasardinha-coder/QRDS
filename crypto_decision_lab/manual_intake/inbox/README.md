# QRDS Manual Intake Inbox

Drop manual/offline research files here for validation.

Accepted formats:

- `.jsonl`
- `.csv`

Required canonical fields:

```text
timestamp, open, high, low, close, volume, symbol, interval, source
```

This inbox is for validation only. Files are not promoted into canonical data directories by default.
