# QRDS/QOS — Dataset Depth Requirements v1

Sprint 9M turns discovered local dataset evidence into explicit depth targets.

This report answers:

- How many structured dataset files are visible?
- How many rows are available?
- Do all requested symbols have local evidence?
- Is the total row depth enough for the next research stage?
- Is per-symbol depth enough?
- Are time-aware files and lineage hashes visible?

The packet is strictly research-only. It cannot unlock trading, allocation, orders, exchange access, or real-capital use.

Expected early result: high-priority depth gaps remain, because the current scanner found only a small row base relative to preliminary depth targets.
