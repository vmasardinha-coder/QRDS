# QRDS/QOS Archive Manifest / Repo Hygiene Index

Sprint 9X records the installer archive created after the repo slimdown work.

Purpose:

- keep old sprint installers traceable;
- keep active root wrappers visible;
- avoid deleting historical installer evidence blindly;
- make future cleanup decisions auditable;
- preserve the research-only policy lock.

The generated portal is available under:

```text
crypto_decision_lab/artifacts/archive_manifest_repo_hygiene/index.html
```

The primary launcher is:

```bash
bash qrds_archive_manifest_repo_hygiene_serve.sh
```

This is documentation/navigation only. It does not enable account access, order flow, execution-style instructions, or live-fund workflow markers.
