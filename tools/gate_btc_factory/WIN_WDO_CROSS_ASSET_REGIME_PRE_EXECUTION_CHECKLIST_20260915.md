# XAWINWDO_REGIME_001 pre-execution checklist

Execution is forbidden until this preregistration bundle is merged with applicable CI green.

After merge, before any economic outcome read:

1. Locate the canonical closed-funnel WIN/WDO physical coverage artifacts.
2. Record exact provenance, hashes, schema, timezone, instrument/roll semantics and synchronized coverage.
3. Prove the prior canonical cost convention applies to the exact new execution semantics; otherwise stop and preregister costs separately.
4. Materialize deterministic discovery/validation/holdout boundaries from eligible pre-cutoff timestamps without inspecting returns in later partitions.
5. Execute discovery once, select at most one static and one regime-conditioned specification, freeze them, then unlock validation.
6. Do not use validation or holdout to add features, change lags, thresholds, windows or regime definitions.
7. Report explicit regime breaks even if headline correlation appears stable.
8. Reject if OOS prediction/net economics/long-regime robustness fail. No retune.
9. Only a historical survivor may receive a separate prospective preregistration. No retroactive credit.
